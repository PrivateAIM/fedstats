# Examples on how to use the code

The `standlone` directory contains examples on using methods directly, the `flame` directory contains examples showing the integration in FLAME.

To run examples
1. Start virtual environment: `poetry env activate`
2. Run script, e.g. `python standalone/multiple_aggregation_cox.py`
